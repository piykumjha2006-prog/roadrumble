# Production build: use same-origin (empty) so the app works when served
# by the FastAPI backend from ANY host: localhost, LAN IP, or HTTPS tunnel.
# Dev server (.env) still points at http://localhost:8000
VITE_API_URL=
